extends layout

block content
  h1= title
  //- p Welcome to #{title}
  if(flag ==1)
    .alert.alert-danger(role='alert') #{message}
  if(flag == 0)
    .alert.alert-success(role='alert') #{message}
  .row
    .col-sm-1
    .col-sm-4
       p.img2
        img(src='/images/xrp.jpeg' alt='login' class="image" style='width:300px; height:250px;')
        //- include pageinfo
    .col-sm-6
      include include/register
    //- .col-sm-1